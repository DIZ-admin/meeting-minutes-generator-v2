# Тестовые переменные окружения для запуска тестов
# Эти значения используются только для тестов и не являются реальными ключами API

# OpenAI API
OPENAI_API_KEY=sk-test-key-not-real-for-testing-only

# Replicate API
REPLICATE_API_TOKEN=r8_test-token-not-real-for-testing-only

# Telegram Bot
TELEGRAM_BOT_TOKEN=test-telegram-token-not-real

# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=meeting_protocol_test
DB_USER=postgres
DB_PASSWORD=postgres

# Пути к директориям
OUTPUT_DIR=./output
LOGS_DIR=./logs
UPLOADS_DIR=./uploads
CACHE_DIR=./cache

# Настройки приложения
TRANSCRIPTION_LANG=de
DEBUG=True
TESTING=True
